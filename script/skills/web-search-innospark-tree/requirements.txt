# =============================================================
# InnoSpark Web Search Skill Tree — 汇总 Python 依赖
# =============================================================
# 安装方式：
#   pip install -r requirements.txt
#
# 可选依赖按需安装（见各分组说明）
# Python 要求：3.8+
# =============================================================


# =============================================================
# 【核心】所有模块共用基础依赖（必装）
# =============================================================
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0
beautifulsoup4>=4.12,<5.0
lxml>=5.0,<6.0
feedparser>=6.0,<7.0


# =============================================================
# 【模块一】Intel Briefing — 情报日报
# 脚本：Intel_Briefing/run_mission.py 等
# =============================================================
httpx[socks]>=0.27,<1.0      # 异步 HTTP 客户端（支持 socks 代理）
google-genai>=1.0,<2.0        # Gemini 中文翻译（可选，无则跳过翻译）


# =============================================================
# 【模块二】Union Search — 多平台联合搜索
# 脚本：union-search-skill/scripts/
# =============================================================
loguru>=0.7.0                  # 结构化日志


# =============================================================
# 【模块三】News Aggregator — 28信源新闻聚合
# 脚本：news-aggregator-skill/scripts/fetch_news.py
# =============================================================
# requests、beautifulsoup4 已在核心依赖中
# playwright 见下方「可选依赖」分组


# =============================================================
# 【模块四】last30days — 30天话题研究
# 脚本：last30days-skill/scripts/last30days.py
# 说明：核心依赖均为标准库 + subprocess 调用 yt-dlp
#       yt-dlp 通过 pip 单独安装（见下方）
# =============================================================


# =============================================================
# 【模块五】UltimateSearchSkill — 双引擎深度搜索
# 脚本：UltimateSearchSkill/scripts/*.sh（Shell 脚本）
# 说明：Shell 脚本依赖 curl + jq（系统工具，非 Python）
#       无需额外 Python 包
# =============================================================


# =============================================================
# 【可选】yt-dlp — YouTube/B站视频字幕（last30days + video-content）
# 建议独立安装以获取最新版本
# =============================================================
yt-dlp>=2024.0.0


# =============================================================
# 【可选】Playwright — HuggingFace Papers / Ben's Bites 信源
# 仅 news-aggregator 的 huggingface / bensbites 信源需要
# 安装后还需运行：playwright install chromium
# =============================================================
# playwright>=1.40.0


# =============================================================
# 【可选】PyGithub — GitHub 仓库/代码搜索
# 仅 union-search 的 github_search.py 需要
# =============================================================
# PyGithub>=2.0.0


# =============================================================
# 【可选】agent-reach — 网页/视频/RSS 访问层
# 含 bird（Twitter）、feedparser 等下游工具
# 建议用 pip install agent-reach 单独安装
# =============================================================
# agent-reach>=0.1.0


# =============================================================
# 【系统工具】非 Python 依赖（需单独安装）
# =============================================================
# curl        — UltimateSearch shell 脚本必需  | brew install curl
# jq          — UltimateSearch shell 脚本必需  | brew install jq
# node / npm  — OpenCLI (JS版) 必需           | brew install node
# opencli-rs  — 已安装到 ~/bin/opencli-rs     | 见 README.md
# gh          — GitHub CLI (web-access模块)   | brew install gh
# bird        — Twitter CLI                   | npm install -g @steipete/bird
